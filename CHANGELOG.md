# 변경 기록

## 미공개

- 마일스톤을 사분면에서 분리해 매트릭스 위 전용 영역에 표시(접기·전체 펼치기). 마일스톤은 사분면 이동·드래그 대상에서 제외하고 rollup 시 마일스톤 하위 작업은 개별 카드로 유지
- 분면 내부를 반응형 카드 그리드로 변경: 분면 실제 너비에 따라 1열↔2열 전환, 카드 최소 300×120px, 카드가 하나여도 한 칸 너비 유지. 카드 정보 위계를 상단(프로젝트·상태 배지) / 가운데(제목) / 하단(마감일·우선순위·태그)으로 정리하고 기본 그림자 제거
- dotpm 2.3.x(구 Project Manager) 호환: 작업 편집기 열기를 dotpm 2.x의 Board `openTask` 경로로, 프로젝트 뷰 갱신을 `loadScope`로, 캐시 갱신을 `store.reloadProject`로 대응. 1.8.x 경로는 기능 존재 여부로 분기해 유지
- dotpm 편집기 설정(`taskEditorSurface`)을 따름: 모달이면 매트릭스 위에 편집 창, 탭이면 dotpm과 같은 `router.openTask` 경로로 편집기 탭 열기. 탭 설정에서는 생성 저장 후 화면 이동 방지 가드를 두지 않음
- 플러그인 설정 연동 상태에 감지된 dotpm 버전과 편집기 설정 표시
- 설정 `dotpm 편집기 Enter로 저장`(기본 꺼짐): dotpm 편집기 안에서만 Enter를 저장으로, dotpm 저장 단축키(Shift+Enter 또는 Ctrl+Enter)를 설명 줄바꿈으로 바꿈. dotpm 설정·코드는 건드리지 않고, 제목·설명칸에 포커스가 있는 동안만 Obsidian 키맵 스택에 우리 Scope 를 올려 처리(Obsidian 키맵이 window 캡처 단계에 먼저 붙어 DOM 리스너로는 dotpm 저장을 앞설 수 없음)
- dotpm 2.x가 작업 저장 시 `projectId`·`parentId`를 `[[경로|제목]]` 위키링크로 쓰는 경우를 대상 노트의 `id`로 풀어 인덱싱. 편집기에서 저장한 카드가 `(프로젝트 없음)`으로 바뀌고 다시 열리지 않던 문제 수정
- 메뉴·알림·설정 문구와 README의 제품명을 dotpm으로 갱신. 내부 플러그인 ID(`project-manager`), 프론트매터 키(`pm-task`, `pm-project`), 이 플러그인의 ID와 설치 폴더는 유지

## 1.8.0

- 카드와 이동 확인 모달의 모든 날짜 뒤에 요일 표시 (예: `2026-09-04 (금)`)
- 카드 우클릭 메뉴에 마감일 빠른 조정 추가: 오늘, 내일, +1일, +1주, 다음 월요일, 마감일 지우기
- 마감일 조정은 사분면 이동과 같은 충돌 검사·되돌리기 경로를 사용하며 완료·보관 작업에는 표시하지 않음

## 1.7.1

- 새 작업 생성 직후 오래된 Project Manager 백그라운드 뷰 때문에 편집 창이 열리지 않던 문제 수정
- Project Manager 캐시에 삭제 대상이 없을 때 프로젝트를 다시 로드하고 실제 삭제 여부를 검증

## 1.7.0

- 작업 카드 우클릭 메뉴에 `작업 삭제` 추가
- 삭제 전 되돌릴 수 없음을 안내하는 확인창 표시
- Project Manager의 작업 삭제 경로를 사용해 작업 파일, 하위 작업, 부모 관계와 프로젝트 데이터를 함께 정리
- 삭제된 작업과 하위 작업을 변화 알림 스냅샷에서도 제거

## 1.6.0

- 완료 작업의 긴급도를 오늘이 아닌 완료일 당시의 마감일 거리로 계산
- 완료 당시 긴급하면서 중요하지 않았던 작업을 `위임` 분면에 표시할 수 있도록 변경
- 완료 당시의 과거 분류가 훼손되지 않도록 완료 카드의 분면 이동을 잠금

## 1.5.2

- Obsidian Native Notice의 강제 배경과 레이아웃 영향을 받지 않는 플러그인 전용 토스트로 교체
- 변화 항목을 전역 버튼 스타일과 분리된 2줄 카드로 렌더링
- 여러 매트릭스 뷰가 열려도 변화 토스트가 하나만 표시되도록 중복 방지

## 1.5.1

- 작업 변화 토스트의 바깥 Notice 폭과 색상을 플러그인 테마에 맞게 재정의
- Obsidian 전역 버튼 높이 때문에 작업 제목과 변화 사유가 겹치던 문제 수정
- 토스트 작업 행을 두 줄 카드로 고정하고 긴 내용은 한 줄 말줄임으로 표시

## 1.5.0

- 매트릭스 상단 공간을 차지하던 작업 변화 브리핑 패널을 Obsidian 토스트 알림으로 변경
- 토스트에 최대 3개 작업과 변화 원인을 간결하게 표시하고 작업 클릭 시 편집기 열기 지원
- `모두 확인`으로 누적 변화를 지우며 동일 내용의 토스트 중복 생성 방지

## 1.4.5

- Obsidian이 백그라운드 Project Manager 탭을 `DeferredView`로 복원한 경우 실제 뷰가 로드되지 않아 작업 편집기를 열지 못하던 문제 수정
- 공식 `WorkspaceLeaf.loadIfDeferred()` 완료 후에만 PM 작업 검색과 편집 호환 경로 실행

## 1.4.4

- 편집 모달을 연 직후 숨은 Project Manager leaf를 닫아 원본 노트 폴백이 발생하던 회귀 수정
- 프로젝트별 숨은 탭을 만들지 않고 단일 편집 호환 leaf를 재사용
- Project Manager 편집 연결 실패 시 원본 Markdown 노트를 자동으로 열지 않도록 변경

## 1.4.3

- 새 작업 저장 후 Project Manager 프로젝트 화면으로 강제 전환되던 동작 차단
- 작업 편집 호환 경로가 임시로 만든 숨은 Project Manager 탭을 사용 후 자동 정리
- 기한 상태 배지와 날짜 칩에서 같은 상대 기한 문구가 중복되던 표시 제거
- 배지가 없는 기본·상세 카드도 고정된 배지 행을 확보해 카드 높이와 정보 위치 통일

## 1.4.2

- 기한 초과·오늘 마감·마감 임박 배지를 카드 밀도와 관계없이 제목 바로 아래의 고정된 두 번째 행에 표시
- 마일스톤 배지도 같은 두 번째 행에 고정해 제목 길이에 따른 줄바꿈 제거
- 기본 카드에서 프로젝트 이름을 숨기고 상세 카드에서만 표시
- 상세 카드의 담당자를 상태·시작일·진행률과 분리된 전용 행에 표시
- 작업 카드 클릭 시 Project Manager 탭으로 전환하지 않고 현재 매트릭스 위에 편집 창 표시

## 1.4.1

- 상세 카드의 상태·시작일·담당자·진행률을 별도 행으로 분리해 기본 카드보다 정보가 적게 보이던 레이아웃 수정

## 1.4.0

- 각 분면의 `+` 버튼에서 Project Manager 작업 생성 창 열기
- 선택한 분면에 맞는 마감일과 우선순위 기본값 자동 입력
- 프로젝트 필터가 하나면 바로 생성하고 여러 프로젝트면 선택 메뉴 표시
- 간결·기본·상세 카드 밀도를 툴바에서 전환하고 설정에 저장
- 상세 카드에 상태·시작일·담당자·진행률 표시

## 1.3.0

- 이전 확인 이후 달라진 작업을 모아 보여주는 분면 이동 브리핑 추가
- 분면·실행 가능 상태·긴급 단계·우선순위·방치 위험의 변화 원인 표시
- 최초 실행은 기준선만 저장하고 사용자가 직접 이동한 작업은 브리핑에서 제외
- 같은 작업의 중복 변화는 최신 항목으로 병합하며 최대 100개까지 안전하게 보관

## 1.2.0

- `Blocked` 상태와 미래 시작일 작업을 사분면 밖 실행 불가 영역으로 분리
- 긴급 작업을 기한 초과·오늘 마감·마감 임박 단계로 표시
- 설정 가능한 기간을 기준으로 방치된 중요 작업 탐지
- 신규 분류 기능의 표시 여부와 판정 기간 설정 추가

## 1.1.1

- 플러그인 작성자 정보를 `SYacuCLoud`로 바로잡음

## 1.1.0

- 완료된 작업을 긴급 분면으로 이동하지 못하도록 모든 입력 경로에서 차단
- self-write 메타데이터 이벤트를 첫 이벤트만 소비하도록 개선
- 카드 키보드 접근성, ARIA 정보, 모바일 길게 누르기 메뉴 추가
- rollup 하위 작업 검색과 긴급·중요·완료 개수 집계 추가
- Project Manager 공개 API capability 탐지와 1.8.x 호환 편집 경로 추가
- 필터·접힘·가상 스크롤로 작업이 보이지 않을 때의 편집기 폴백 보강

## 1.0.0

- 최초 공개 버전
